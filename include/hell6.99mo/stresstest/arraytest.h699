@import stresstest/level2.h699
# @import stresstest/level1.h669

# Strings
user:
    name = "ghgltggamer"
    status = "active"

# Numbers
    age = 21
    score = 9000

# Booleans
    verified = true
    admin = false

# Arrays
    fruits = ["apple", "banana", "cherry"]
    numbers = ["one", "two", "three"]
    str = [
        "Str1",
        "Str2",
        "Str3"
    ]

# UNIDEF
token = UNIDEF
